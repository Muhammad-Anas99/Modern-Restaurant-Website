/**
 * Point this at your deployed backend before publishing. Leave it as '/api' only if
 * you deploy the frontend and backend together behind the same domain (they aren't,
 * by default, in this project — the backend is its own separate Vercel project).
 *
 * Example once your backend is live on Vercel:
 *   window.__API_BASE__ = 'https://foundry-flame-backend.vercel.app/api';
 */
window.__API_BASE__ = 'https://your-backend-project.vercel.app/api';
