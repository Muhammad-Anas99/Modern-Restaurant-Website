/**
 * Point this at your backend API.
 *
 * LOCAL TESTING (backend running via `npm run dev` on your machine):
 *   window.__API_BASE__ = 'http://localhost:5000/api';
 *
 * PRODUCTION (once your backend is deployed on Vercel):
 *   window.__API_BASE__ = 'https://your-backend-project.vercel.app/api';
 *
 * Swap the line below when you move from local testing to publishing.
 */
// window.__API_BASE__ = 'http://localhost:5000/api';           // local
window.__API_BASE__ = 'https://modern-restaurant-website-gamma.vercel.app/api';  // production
